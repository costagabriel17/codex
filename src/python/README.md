# Python

Pipelines de dados e imagens do projeto.

Regras:

- usar Python 3.11+
- ler segredos apenas de `.env` local
- gerar `summary.json` em `reports\`
- nao salvar saidas finais dentro desta pasta
