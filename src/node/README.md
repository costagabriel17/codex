# Node

Esta pasta contem scripts operacionais canônicos do projeto.

Padrao:

- usar `process.env` carregado de `.env` local
- gerar `summary.json` em `reports\`
- priorizar Shopify Admin GraphQL
- implementar fallback resiliente quando a API variar
